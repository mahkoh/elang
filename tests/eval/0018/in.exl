let
    f = a: a + 1,
    g = a @ { x }: x + 10,
    h = { x }: x + 100,
    i = { x ? 1000 }: x + 1000,
    j = { x, .. }: (x + 10000),
    k = _: 200000,
in
    f 1 + g { x = 10 } + h { x = 100 } + i { } + j { x = 10000, inherit f } + k _
